@{
    ViewData["Title"] = "Create First Admin User";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="fas fa-user-shield me-2"></i>
                        Tạo Admin User Đầu Tiên
                    </h4>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(ViewBag.SuccessMessage))
                    {
                        <div class="alert alert-success">
                            <i class="fas fa-check-circle me-2"></i>
                            @ViewBag.SuccessMessage
                        </div>
                        <div class="text-center">
                            <a href="/Auth/Login" class="btn btn-primary">
                                <i class="fas fa-sign-in-alt me-2"></i>
                                Đăng nhập Admin
                            </a>
                        </div>
                    }
                    else
                    {
                        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                        {
                            <div class="alert alert-danger">
                                <i class="fas fa-exclamation-triangle me-2"></i>
                                @ViewBag.ErrorMessage
                            </div>
                        }

                        <div class="alert alert-info">
                            <i class="fas fa-info-circle me-2"></i>
                            <strong>Lưu ý:</strong> Trang này chỉ sử dụng để tạo admin user đầu tiên cho hệ thống. 
                            Sau khi tạo xong, vui lòng tắt tính năng setup trong appsettings.json.
                        </div>

                        <form method="post">
                            <div class="mb-3">
                                <label for="email" class="form-label">
                                    <i class="fas fa-envelope me-2"></i>
                                    Email Admin
                                </label>
                                <input type="email" class="form-control" id="email" name="email" required 
                                       placeholder="admin@monamour.com">
                            </div>

                            <div class="mb-3">
                                <label for="password" class="form-label">
                                    <i class="fas fa-lock me-2"></i>
                                    Mật khẩu
                                </label>
                                <input type="password" class="form-control" id="password" name="password" required 
                                       placeholder="Mật khẩu mạnh" minlength="6">
                                <div class="form-text">Mật khẩu phải có ít nhất 6 ký tự</div>
                            </div>

                            <div class="mb-3">
                                <label for="fullName" class="form-label">
                                    <i class="fas fa-user me-2"></i>
                                    Họ và tên
                                </label>
                                <input type="text" class="form-control" id="fullName" name="fullName" required 
                                       placeholder="Tên admin">
                            </div>

                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary btn-lg">
                                    <i class="fas fa-user-plus me-2"></i>
                                    Tạo Admin User
                                </button>
                            </div>
                        </form>

                        <hr>
                        <div class="text-center">
                            <a href="/Setup/TestRoles" class="btn btn-outline-info">
                                <i class="fas fa-vial me-2"></i>
                                Test System
                            </a>
                            <a href="/" class="btn btn-outline-secondary">
                                <i class="fas fa-home me-2"></i>
                                Về trang chủ
                            </a>
                        </div>
                    }
                </div>
            </div>

            <div class="mt-4">
                <div class="card">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="fas fa-question-circle me-2"></i>
                            Hướng dẫn
                        </h6>
                    </div>
                    <div class="card-body">
                        <ol>
                            <li>Điền thông tin admin user đầu tiên</li>
                            <li>Admin user sẽ được tạo và tự động xác thực email</li>
                            <li>Đăng nhập với tài khoản admin vừa tạo</li>
                            <li>Truy cập <strong>/Admin/Dashboard</strong> để quản lý hệ thống</li>
                            <li>Tắt tính năng setup trong appsettings.json sau khi hoàn tất</li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
